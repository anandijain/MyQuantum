
# this is used to call defns and thms quickly. just an idea but might build out more

function unitary()
    print("""A complex square matrix U is unitary if U†U == UU† == Id
    
    Properties of unitary matricies:

    * diagonalizable (SVD)
    * norm-preserving
    * invertible
    """)
end
